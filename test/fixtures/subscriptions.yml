# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

subscription_one:
  user: user_one
  publisher: rss_channel_one (RssChannel)

subscription_two:
  user: user_one
  publisher: user_one (User)

subscription_three:
  user: user_one
  publisher: user_two (User)

subscription_four:
  user: user_two
  publisher: user_two (User)
